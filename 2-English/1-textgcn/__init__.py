from __future__ import print_function
# 即使在python2.X, 使用print函数也同python3.X相同, 即加括号使用
from __future__ import division
# 导入python未来支持的语言特征division(精确除法)
# 当没有在程序中导入该特征时, "/"操作符执行的是截断除法(Truncating Division)
# 当导入精确除法后, "/"执行精确除法操作, "//"执行截断除法